class Solution {
    int findMissingElement(int[] array, int N) {
        int sum = N * (N + 1) / 2;
        for (int i = 0; i < array.length; i++) {
            sum -= array[i];
        }
        return sum;
    }
}
